events {}

http {
    server {
        listen 80;
        # server_name api.idk-yet.com;
        # server_name idkyet.kodit.com.mx;
        server_name _;

        # location /.well-known/acme-challenge/ {
        #     root /var/www/certbot;
        # }

        # Before installing the certificate uncomment
        location /api/ {
            proxy_pass http://api-gateway:3000;
            proxy_http_version 1.1;
            proxy_set_header Host $host;
            proxy_set_header X-Real-IP $remote_addr;
            proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
            proxy_set_header X-Forwarded-Proto $scheme;
            proxy_set_header Upgrade $http_upgrade;
            proxy_set_header Connection "upgrade";
            proxy_cache_bypass $http_upgrade;
        }

        location / {
            root /usr/share/nginx/html;
            index index.html;
            try_files $uri $uri/ /index.html;
        }

        # # Comment when the certificate is not installed
        # location / {
        #     return 301 https://$host$request_uri;
        # }
    }

    # # Comment when the certificate is not installed
    # server {
    #     listen 443 ssl;
    #     server_name api.idk-yet.com;
    #     # server_name idkyet.kodit.com.mx;

    #     # ssl_certificate /etc/letsencrypt/live/idkyet.kodit.com.mx/fullchain.pem;
    #     # ssl_certificate_key /etc/letsencrypt/live/idkyet.kodit.com.mx/privkey.pem;
    #     ssl_certificate /etc/letsencrypt/live/api.idk-yet.com/fullchain.pem;
    #     ssl_certificate_key /etc/letsencrypt/live/api.idk-yet.com/privkey.pem;

    #     location / {
    #         proxy_pass http://api-gateway:3000;
    #         proxy_http_version 1.1;
    #         proxy_set_header Host $host;
    #         proxy_set_header X-Real-IP $remote_addr;
    #         proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
    #         proxy_set_header X-Forwarded-Proto $scheme;
    #     }
    # }
}
